MALTEN_ADDR=127.0.0.1:8080
# Required for sharing. Existing anthropic_key file also works.
ANTHROPIC_API_KEY=
# Optional:
MALTEN_MODERATION_MODEL=claude-sonnet-5

# Persistent bounded stream (relative to the service working directory):
MALTEN_DATA=data/stream.json
